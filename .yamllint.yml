---
extends: default

rules:
  line-length:
    max: 120
  document-start:
    # Kpt will remove leading '---' post-processing
    ignore: |
      applications/**/*.yaml
      applications/**/Kptfile
