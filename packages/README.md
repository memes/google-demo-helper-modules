# Packages

This folder contains a set of kubernetes packages that have been used in F5 and Google Cloud demos for the past few
years. Each package is typically represents the upstream original application with no (or minimal) changes from the
authors original publication. I.e. the images are pulled from the same container repository as the author's original
manifests.
